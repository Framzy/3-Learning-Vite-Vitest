{
  "name": "vitest-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "bench": "vitest bench --run tests/hello.bench.ts",
    "test": "vitest --run tests/",
    "coverage": "vitest --run --coverage tests/"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@vitest/coverage-istanbul": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "typescript": "^5.9.3",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  }
}
